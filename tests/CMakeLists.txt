# Different unit tests that should be run to check if everything is OK.
SET(ATOM_MOLECULE_TEST_CXX 
    ${CMAKE_CURRENT_SOURCE_DIR}/atom_molecule.cpp 
    CACHE INTERNAL "Test of the atom and molecule classes." FORCE)

